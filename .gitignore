# Dependencies
node_modules/

# Local secrets (prod secrets live as Convex env vars, not in the repo)
.env
.env.local
.env.production

# Logs / misc
*.log
.DS_Store

# Note: convex/_generated/ IS committed (see Convex docs: "your code won't
# typecheck without it") — it is regenerated by `npx convex dev`/`codegen`.
